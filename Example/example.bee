enum SomeEnum : byte {
	Zero,
	One,

	# Explicitly define enum values
	Five		= 5,
	Eight		= 8,
	EleventyOne	= 111
}

# Define a custom structure
struct SomeStruct {
	# A unique identifier
	an_id	: guid;
	bytes	: byte[4];
}

schema {
	# Some primitive fields
	a_byte			: byte;
	a_short			: short;
	an_int			: int;
	a_long			: long;
	a_float,
	another_float	: float;

	# Some complex structures
	a_struct		: SomeStruct;
	an_enum			: SomeEnum;
}
